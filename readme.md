# Typescript

typescript는 최종적으로 javascript로 변환된다. 노드, 브라우저는 기본적으로 javascript로만 실행 가능. 컴파일러(tsc)로 javascript로 변환해야함.

tsc: 코드 타입 검사, javascript로 변환


npm init -y : package.json 설치

npm i typescript: typescript 설치

npx tsc --init : tsconfig.json 설치

npx tsc --noEmit : 타입 검사

npx tsc : ts -> js 변환